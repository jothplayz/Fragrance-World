{
  "name": "Fragrance Wardrobe",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20-bookworm",
  "forwardPorts": [3000],
  "postCreateCommand": "npm install && npx prisma generate && (test -f .env || cp env.example .env) && npx prisma db push",
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint", "Prisma.prisma"]
    }
  }
}
