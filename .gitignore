# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# portable Node (Windows ZIP extract)
/.tools/
/node-v*-win-x64/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/*.db
prisma/*.db-journal
# Team DB lives at repo root data/shared.db only — not under prisma/data/
prisma/data/

# local-only sqlite at repo root (DATABASE_URL=file:../dev.db — path is relative to prisma/)
dev.db
dev.db-journal

# data/shared.db is the committed team database (see env.example)